export const MAX_PICK = 10;
export const PICKER_SEARCH_DEBOUNCE_MS = 300;

/** Shared grid template so the picker header and every row keep their columns aligned. */
export const PICKER_ROW_GRID = 'grid grid-cols-[1.5rem_minmax(7rem,1.4fr)_minmax(0,2fr)_5rem] items-start gap-3';
